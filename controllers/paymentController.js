const Payment = require('../models/Payment');
const Order = require('../models/Order');

const processPayment = async (req, res) => {
  // logic 
  // paymentGateway call here
};

module.exports = { processPayment };
